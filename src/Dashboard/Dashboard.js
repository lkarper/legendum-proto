import React, { useContext } from 'react';
import NotesList from '../NotesList/NotesList';

const Dashboard = () => {

    return (
        <div>
            <NotesList />

        </div>
    );
}

export default Dashboard;